import { MissingDataPipe } from './missing-data.pipe';

describe('MissingDataPipe', () => {
  it('create an instance', () => {
    const pipe = new MissingDataPipe();
    expect(pipe).toBeTruthy();
  });
});
